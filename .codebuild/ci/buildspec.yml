version: 0.2
phases:
  pre_build: 
    commands:
      - mv diff_target diff_target_old
      - git reset --hard HEAD
  build:
    commands:
      - echo これが表示されるだけ、のはずなんですがねぇ。しらんけど。
      - find .
      - diff ./diff_target/change.txt ./diff_target_old/change.txt || echo "change.txtに変化が在った。"
cache:
  paths:
    - diff_target/*
